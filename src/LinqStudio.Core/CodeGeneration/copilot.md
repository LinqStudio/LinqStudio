# Core code generation

This directory contains internal schema normalization and source-rendering helpers used by `DbContextGenerator`. `GeneratedSchema` is the internal normalized representation where database foreign keys can later be combined with project-defined relationships before model and DbContext rendering.
