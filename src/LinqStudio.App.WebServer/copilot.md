# LinqStudio.App.WebServer

ASP.NET Core Blazor Server host for LinqStudio.
Projects stored in `~/.linqstudio/` (or configured path) on the server using `FileSystemProjectRepository`/`FileSystemQueryRepository` (LinqStudio.Core).
No native file dialogs — project browser is a Blazor dialog (`ProjectBrowserDialog`).
