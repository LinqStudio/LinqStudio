# LinqStudio.DatabaseSeeder

Console app that seeds demo databases (MSSQL + MySQL) with sample data on Aspire startup. Reads connection strings from environment variables injected by Aspire orchestration.
